; Tests for sweeten.
xyz
(simplelist)
(a)(b)
; Simple comment.

; Comment preceded by blank line.
(x y z (qq xx)) (m n o)
(E F g)

  (define (f a) (* 2 a))

(define (q x) (-> x 1))

(define (g c d e)
   (really-long-name c)
   (really-long-name d)
   (really-long-name d)
   (really-long-name e))

(define (factorial n)
  (if (<= n 1)
      1
      (* n (factorial (- n 1)))))

(define (g x)
  (q (* 2 x)))

(define (h x)
  (set! z (r (* 2 x y))))

(set! q (f (- x)))

(defun f (a b)
  (boggle a)
  (boggle b))
; Comment at end of file.
